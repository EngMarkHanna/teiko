"""Analysis package for the Loblaw Bio cell-count take-home.

Each module maps to one part of the assignment and is imported by both
run_pipeline.py (batch outputs) and dashboard.py (interactive view), so the
analysis logic lives in exactly one place.
"""
